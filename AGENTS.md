# AGENTS.md

This is a new project. There are no existing users, deployments, or data to preserve.

- **No backwards compatibility is needed.** Change or remove APIs, config formats, data schemas, and file layouts freely.
- Don't add deprecation shims, migration code, compatibility layers, or fallback paths for old behavior.
- Prefer the simplest, cleanest design over preserving existing interfaces. Delete dead code rather than keeping it around.
